# AGENT CONTEXT — PART

Utopia landing + cream/ink trader desk on **Fable 5.1**. Robinhood Chain only (EIP-155 `4663`). Palette: `#f3ead7` on `#05080c`.

Do not publish author names, GitHub handles, or personal remotes in the product UI.

Run: `npm install && npm run dev`

## Product

1. Highway landing. Scroll or Enter Terminal. Docs and Add Extension open in-page sheets.
2. Terminal is a Fable 5.1 desk. Search + token + intel + swap on the left. Chart + brief on the right. Pulse / Blocks / Bubble Map + tape on the floor.
3. Bubble Map nodes are stippled like the sitting figure, webbed, with a light glitch. No old mascot. No socials rail.
4. Charts stay empty until the user picks a token.

## Extension

`public/part-ext.zip` — unpacked Chrome install. Copy lives in `extension/`.
